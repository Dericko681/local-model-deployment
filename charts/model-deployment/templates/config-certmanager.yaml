{{- if .Values.certManager.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: config-certmanager
  namespace: knative-serving
  labels:
    app.kubernetes.io/part-of: knative-serving
    app.kubernetes.io/component: certmanager
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
data:
  issuerRef: |
    name: {{ .Values.certManager.knative.issuerRefs.external.name | quote }}
    kind: {{ .Values.certManager.knative.issuerRefs.external.kind | quote }}
  clusterLocalIssuerRef: |
    name: {{ .Values.certManager.knative.issuerRefs.clusterLocal.name | quote }}
    kind: {{ .Values.certManager.knative.issuerRefs.clusterLocal.kind | quote }}
  systemInternalIssuerRef: |
    name: {{ .Values.certManager.knative.issuerRefs.systemInternal.name | quote }}
    kind: {{ .Values.certManager.knative.issuerRefs.systemInternal.kind | quote }}
{{- end }}
